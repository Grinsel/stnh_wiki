:{$PORT} {
	root * .
	file_server
	try_files {path} {path}/ /index.html
	encode gzip

	@assets {
		path /icons/* /assets/* /js/* /css/*
	}
	header @assets Cache-Control "public, max-age=31536000, immutable"

	@html {
		path *.html /
	}
	header @html Cache-Control "no-cache, must-revalidate"
}
